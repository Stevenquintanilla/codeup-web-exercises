"use strict";

var rectangleLength = prompt("What is the length of the rectangle?"),
    rectanleWidth = prompt("What is the width of the rectangle?"),
    userInputs = alert("You entered dimensions of " + rectangleLength + "by")